Introduction:

It creates a virtual serial port (COM) or parallel port (LPT), and the user can
access the USB printer through that port.


Steps for using:

1. Install a virtual port. Then select serial port or parallel port.
2. Use the port as a real port.
